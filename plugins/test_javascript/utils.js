// Constants
var PLUGIN_TAG = "test_javascript";
var TAG_MENUACTION = "test";
var TAG_CODE = "code";
var TAG_SAVE_OPTIONS = "save-options";
var TIMER_DELAY = 60000;


// A helper function
function buildTag(action) {
    return PLUGIN_TAG + ":" + action;
}
